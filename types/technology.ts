export type TechnologyCard = {
  title: string;
  description: string;
};

export type TechnologySignal = {
  title: string;
  description: string;
};

export type TrackingStep = {
  title: string;
  description: string;
};