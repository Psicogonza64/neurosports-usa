export type WhatWeDoCard = {
  title: string;
  description: string;
  marker: string;
};

export type BrainModelPhase = {
  title: string;
  note: string;
};

export type SportsProgram = {
  name: string;
  description: string;
};